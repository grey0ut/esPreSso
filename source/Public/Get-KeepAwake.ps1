function Get-KeepAwake {
    <#
    .SYNOPSIS
    Checks the registered scheduled jobs for the esPreSso job that runs Start-KeepAwake
    .DESCRIPTION
    Checks the registered scheduled jobs for the esPreSso job that runs Start-KeepAwake and return an object representing its settings

    #>
    [CmdletBinding()]
    param (

    )
}
